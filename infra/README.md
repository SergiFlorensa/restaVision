# Infra

Elementos de infraestructura y soporte operativo:
- `docker/` para contenedores y composición.
- `scripts/` para utilidades reproducibles.
- `db/` para inicialización y cambios de base de datos.

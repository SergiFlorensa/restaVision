# Models

Activos relacionados con modelos:
- `checkpoints/` para pesos y snapshots.
- `exported/` para modelos convertidos a formatos de despliegue.
- `metadata/` para metadatos, métricas y manifiestos ligeros.

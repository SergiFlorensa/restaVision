# Apps

Aplicaciones de entrada y salida del sistema:
- `api/` para la API y orquestación backend.
- `dashboard/` para la interfaz operativa.
- `worker/` para procesos auxiliares y tareas en segundo plano.
